plugins {
    id 'groovy-gradle-plugin' 
}

repositories {
    gradlePluginPortal()
}

dependencies {
    // Makes the 'com.vanniktech.maven.publish' plugin applyable from the convention plugin
    implementation 'com.vanniktech:gradle-maven-publish-plugin:0.37.0'
}
