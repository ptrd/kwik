plugins {
    id 'buildlogic.java-common-conventions'
}

dependencies {
    implementation(project(':kwik')) // Add kwik core as a dependency
}

mavenPublishing {
    pom {
        name = 'Kwik H09'
        description = 'HTTP/0.9 implementation running on top of Kwik, the QUIC implementation in Java'
    }
}
