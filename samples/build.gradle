plugins {
    id 'buildlogic.java-common-conventions'
}

dependencies {
    implementation(project(':kwik')) // Add kwik core as a dependency
}

mavenPublishing {
    pom {
        name = 'Kwik Samples'
        description = 'Samples for Kwik, the QUIC implementation in Java'
    }
}
